#pragma once

// Compatibility umbrella for the DSi override table used by PlatformTuning.h.
// Only the chunk-streaming/cache knobs exist so far -- see DsiWorldTuning.h for
// why the render/mesh-timing group isn't here yet.
#include "dsi/tuning/DsiWorldTuning.h"
